lis方法：
    １．o(n^2) 
        分别计算以每个下标结束的最长升序序列的长度，
        第i个位置的值res[i] = max(res[j]+1, res[i])( start<=j<i && data[i]>data[j]) 
        查找时间为o(n)
    ２．o(nlgn)
        思想同上，但是用二分查找，可以使得查找时间变为 o(lgn) 
